$(".email-obfuscator").each(function(e){
    data = JSON.parse(e.innerHTML);
    e.innerHTML = '<a href="mailto:'+data.name+'@'+data.domain+'.'data.tld'">'+data.name+'&#64;'+data.domain+'&#46;'data.tld'</a>';
  })
