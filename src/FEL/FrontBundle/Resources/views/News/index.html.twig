
        {% for n in news %}

            <article>
                <h3>{{ n.title }}</h3>
                <p>{{ n.content|raw }}</p>
                {# <footer>
                    <i class="glyphicon glyphicon-time"></i>
                    <em>{{ n.date|date("l, \\t\\h\\e jS F o \\a\\t g:ia") }}</em>,
                    from <strong>{{ n.author }}</strong>.
                </footer>#}
            </article>

        {% else %}
            <div class="alert alert-info" role="alert">Aucune News n'a été trouvée.</div>
        {% endfor %}

