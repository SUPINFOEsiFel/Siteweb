{% extends "FELAppBundle:Default:layout.html.twig" %}
{% block body %}
<h1>Ajouter un article</h1>

{% if message %}
<p>{{ message }}</p>
{% endif %}

<form action="" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" />
</form>

{% endblock %}